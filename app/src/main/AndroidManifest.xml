<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="nl.surf.filesender.rde.client">

    <uses-permission android:name="android.permission.NFC" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <uses-feature
        android:name="android.hardware.nfc"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SURFfilesenderRDEclientAndroid"
        tools:targetApi="31">

        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.decryption.DecryptionParamsInputActivity"
            android:exported="true" />
        <activity
            android:name=".activities.decryption.DecryptionReadMRZActivity"
            android:exported="true" />
        <activity
            android:name=".activities.decryption.ExtractDecryptionKeyReadNFCActivity"
            android:exported="false" />
        <activity
            android:name=".activities.encryption.EncryptionParamsInputActivity"
            android:exported="true" />
        <activity
            android:name=".activities.encryption.EncryptionActivity"
            android:exported="false" />
        <activity
            android:name=".activities.enrollment.EnrollmentScanQRActivity"
            android:exported="true" />
        <activity
            android:name=".activities.enrollment.EnrollmentReadMRZActivity"
            android:exported="false" />
        <activity
            android:name=".activities.enrollment.EnrollmentReadNFCActivity"
            android:exported="false" />
        <activity
            android:name=".activities.enrollment.EnrollmentResultActivity"
            android:exported="false" />
    </application>

</manifest>